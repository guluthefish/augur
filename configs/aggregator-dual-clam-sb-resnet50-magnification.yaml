extends: common-params.yaml
name: DualCLAM
params:
  enc_dim: 2048
  main_task: subtyping
  pretext_tasks: ["sbs_regression"]
  hidden_dims: [256, 128]
  output_dims:
    subtyping: 8
    sbs_regression: 97
  dropout: 0.2
  attn_kwargs:
    multi_branch: false
    gated: false
    hidden_dim: 256
    dropout: 0.25
  cluster_kwargs:
    k_sample: 8
    inst_weight: 0.25
    out_of_class: true
checkpoint_path: checkpoints/dual-clam-sb-resnet50-magnification.pth
