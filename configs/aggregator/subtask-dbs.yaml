# DBS (doublet-base substitution) COSMIC signature regression subtask.
# output_dims.dbs_regression is the number of signature columns in the
# contributions TSV — verify against your actual labels file before training.
params:
  subtasks: ["dbs_regression"]
  output_dims:
    dbs_regression: 22
  task_weights:
    dbs_regression: 1.0
