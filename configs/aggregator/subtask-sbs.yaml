# SBS (single-base substitution) COSMIC signature regression subtask.
# output_dims.sbs_regression is the number of signature columns in the
# contributions TSV.
params:
  subtasks: ["sbs_regression"]
  output_dims:
    sbs_regression: 97
  task_weights:
    sbs_regression: 1.0
